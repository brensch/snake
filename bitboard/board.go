package bitboard

import "github.com/brensch/snake/rules"

type Board struct {
	MaxerHigh uint64
	MaxerLow  uint64
}

func BoardFromState(state *rules.BoardState) *Board {

	// var high, low uint64

	// for _, snake := range state.Snakes {
	// 	// if snake.
	// }

	return nil
}
